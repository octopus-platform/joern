<?php

// bit operators
$or1 | $or2;
$and1 & $and2;
$msg ^ $otp;
$x << $y;
$x >> $y;
// string operators
$str1 . $str2;
// arithmetic operators
$x + $y;
$x - $y;
$x * $y;
$x / $y;
$x % $y;
$x ** $y;
// boolean operators
$x xor $y;
$x && $y;
$x || $y;
// comparison operators
$x === $y;
$x !== $y;
$x == $y;
$x != $y;
$x < $y;
$x <= $y;
$x <=> $y;
$x > $y;
$x >= $y;
